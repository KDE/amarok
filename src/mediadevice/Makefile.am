if with_libgpod
    IPOD_SUBDIR = ipod
endif

if with_ifp
    IFP_SUBDIR = ifp
endif

VFAT_SUBDIR = generic

METASOURCES = AUTO

SUBDIRS = \
    $(IPOD_SUBDIR) \
    $(IFP_SUBDIR) \
    $(GENERIC_SUBDIR)

