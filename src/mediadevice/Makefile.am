if with_libgpod
    IPOD_SUBDIR = ipod
endif

if with_ifp
    IFP_SUBDIR = ifp
endif

if with_libnjb
    NJB_SUBDIR = njb
endif

METASOURCES = AUTO

SUBDIRS = generic $(IPOD_SUBDIR) $(IFP_SUBDIR) $(NJB_SUBDIR)

